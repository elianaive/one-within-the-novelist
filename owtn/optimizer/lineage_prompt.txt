You are distilling evaluator feedback on {domain_hints}. The subject has been compared head-to-head against others in one or more pairwise tournament matches. Judges analyzed each match across {rubric_summary}. Your job is to extract a structured critique of THIS LINEAGE — what reviewers found wrong with it, what they found strong about it, what patterns it exhibits, and what a successor could try differently.

Key rules:

1. Extract only from what the judges actually wrote. Do not invent critiques, do not extrapolate beyond the evidence, do not manufacture "established weaknesses" that were mentioned by only one judge in one match.
2. "Established" means the critique appeared across multiple matches OR was voiced by multiple judges within a match. A one-off remark is not established.
3. Each match block tells you explicitly which concept was THIS LINEAGE and which was the OPPONENT. The judges' reasoning refers to concepts directly as "THIS LINEAGE" / "OPPONENT" throughout (these labels have been pre-resolved from the judges' own internal fingerprints). Focus on what was said about THIS LINEAGE, not about the OPPONENT.
4. If this lineage won a match, judges' praise for it is signal — use it to populate `contested_strengths` (if others disagreed) or as context for `attractor_signature`. But "contested strengths" must reflect genuine disagreement across judges, not just a lone praise.
5. `attractor_signature` names the specific motifs, structural moves, or patterns THIS LINEAGE relies on that have drawn critical attention. {attractor_examples} Keep items concrete and operational — patterns a successor can recognize and avoid.
6. `divergence_directions` is prescriptive AND must address BOTH established weaknesses AND attractor_signature items. Phrase each as an explicit prohibition: "Do not produce a {subject_noun} that [pattern from attractor_signature]." or "Avoid [specific motif this lineage relied on]." Every item in `attractor_signature` must have at least one corresponding "do not" entry in `divergence_directions` — these are the patterns the next {subject_noun} must NOT replicate. Additionally include positive direction items framed as "Successor must instead [concrete alternative]" when an alternative is suggested by the critique. Ground every direction in either a specific established_weakness or a specific attractor_signature item.
7. Keep bullets tight. 1-2 sentences per bullet max. No preamble, no summary.
8. If the evidence is thin (e.g. only one match, only one judge critiqued cleanly), err on fewer bullets rather than padding. An empty list is preferable to invented content.

Respond with a single JSON object matching the {output_schema_name} schema with four fields: established_weaknesses, contested_strengths, attractor_signature, divergence_directions. Each is a list of strings.
