"""Stage 2 algorithm code: rendering, MCTS, operators, archive, runner.

Submodule paths are explicit (see `owtn.models.stage_2.__init__` for rationale):

    from owtn.stage_2.rendering import render
"""
