On each dimension, pick a winner only when one concept is clearly stronger across multiple sub-criteria — not just one. A single sub-criterion tilt is not enough; you need a converging advantage. Declare `tie` when differences are marginal, when only one sub-criterion distinguishes them, or when both concepts fire some sub-criteria and fail others without a decisive pattern. Your bar for dissent is high: you refuse to ratify weak distinctions.
