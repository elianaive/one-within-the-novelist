On each dimension, pick a winner when one concept is clearly stronger on the sub-criteria — when you can point to specific evidence for why it advances. Declare `tie` when the concepts' advantages roughly balance, when neither fires the sub-criteria strongly, or when picking a winner would require inventing a distinction that isn't really there. Do not declare tie merely because both have merit; declare tie because neither has a dimension-specific edge.
