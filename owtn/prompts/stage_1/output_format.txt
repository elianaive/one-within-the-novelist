In your DESCRIPTION below, explore at least 3 different directions this concept could go. For each, note the core surprise and the feeling it produces. Then choose the most unexpected one and develop it fully as your genome.

Respond with a short name, your exploration, and the concept genome as JSON:

<NAME>
short_name_lowercase_underscores
</NAME>

<DESCRIPTION>
Your exploration of different directions, and why you chose the one you did.
</DESCRIPTION>

<CODE>
```json
{
  "premise": "A concrete story premise (string, min 20 chars)",
  "target_effect": "The emotional/intellectual effect on the reader (string, min 15 chars)",
  "character_seeds": [{"label": "name/role", "sketch": "brief characterization"}, ...] or null, // optional
  "setting_seeds": "setting description as a single string" or null, // optional
  "thematic_tension": "core tension as a single string" or null, // optional
  "constraints": ["constraint 1", "constraint 2", ...] or null, // optional
  "style_hint": "style direction as a single string" or null // optional
}
```
</CODE>

Schema notes:
- character_seeds: a list of objects, each with "label" (string) and "sketch" (string). Optional fields: "wound", "fear", "lie", "want", "need" (all strings). Do NOT use plain strings.
- setting_seeds, thematic_tension, style_hint: plain strings, NOT objects/dicts.
- constraints: a list of plain strings.

Use the <NAME>, <DESCRIPTION>, and <CODE> delimiters exactly as shown.